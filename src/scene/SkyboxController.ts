import { BasicController } from "./BasicController";

export class SkyboxController extends BasicController {
  public update(delta: number) {
    // this.parent.translation = this.scene.camera.translation;
  }
}